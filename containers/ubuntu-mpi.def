Bootstrap: docker
From: ubuntu:22.04

%files
    containers/mpitest.c /opt/mpitest.c

%post
    chmod +x /opt/mpitest.c

%environment
    export PATH="$MPI_DIR/bin:$PATH"
    export LD_LIBRARY_PATH="$MPI_DIR/lib:$LD_LIBRARY_PATH"

