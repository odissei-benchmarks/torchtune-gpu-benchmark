Bootstrap: docker
From: ubuntu:22.04

%files
    containers/mpitest /opt/mpitest

%post
    chmod +x /opt/mpitest

%environment
    export PATH="$MPI_DIR/bin:$PATH"
    export LD_LIBRARY_PATH="$MPI_DIR/lib:$LD_LIBRARY_PATH"

